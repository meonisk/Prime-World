# No Git LFS filters — all heavy binary files are delivered via external zip
# archives on Google Drive and fetched by tools/assets/fetch_assets.py.
# See tools/assets/manifest.json for the full list.
